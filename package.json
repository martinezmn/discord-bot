{
  "name": "bot-mega",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "tsc && tsc-watch --onSuccess \"node ./dist/server.js\"",
    "build": "tsc",
    "test": "DATABASE_URL=file:../test.db prisma migrate deploy && DATABASE_URL=file:../test.db vitest"
  },
  "devDependencies": {
    "@types/cron": "^2.0.0",
    "prisma": "^4.10.1",
    "tsc-watch": "^6.0.0",
    "vitest": "^0.28.4"
  },
  "dependencies": {
    "@prisma/client": "4.10.1",
    "cron": "^2.2.0",
    "discord.js": "^14.7.1",
    "dotenv": "^16.0.3",
    "moment": "^2.29.4",
    "typescript": "^4.9.5"
  }
}
